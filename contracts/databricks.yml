bundle:
  name: learning-data-contract-repo

workspace:
  # Framework notebooks default their contract_repo_root widget to this path.
  file_path: /Workspace/Shared/data_contract_repo

targets:
  vscodedev:
    mode: development
    default: true